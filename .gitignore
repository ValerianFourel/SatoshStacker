# secrets
.env
.env.local
*.key
*.pem
*.p12
# exported API-key CSVs are SECRETS — never commit them
*apiKey*.csv
*api-key*.csv
Default Workspace-apiKey-344190.csv

# runtime state (snapshots, detector cooldowns, onboarding marker) — regenerated
state/

# state / db
*.db
*.sqlite
*.sqlite3
*.paperbook.json

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/

# large data (re-fetch via backtest/fetch_data.py)
backtest/data/

# keep backtest result CSVs out of git noise (regenerate via engine.py)
backtest/results/*.csv
